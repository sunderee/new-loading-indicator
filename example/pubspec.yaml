name: example
description: "Showcasing the usage of the new_loading_indicator package"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.9.2"
  flutter: ">=3.35.6"

dependencies:
  flutter:
    sdk: flutter
  new_loading_indicator:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true